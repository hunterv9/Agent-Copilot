name: validate-agents
on: [push, pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate agents
        shell: pwsh
        run: ./scripts/validate-agents-ci.ps1
